# ⚡ البدء السريع - نظام خطط الأسعار

**وقت الإعداد:** 5 دقائق فقط! ⏱️

---

## 🎯 الهدف

إعداد نظام خطط الأسعار الكامل في AS Hub بأسرع طريقة ممكنة.

---

## 🚀 3 خطوات فقط

### الخطوة 1: تحديث قاعدة البيانات (دقيقتان)

#### إذا كانت القاعدة جديدة:
```bash
mysql -u username -p database_name < database/schema_updated.sql
```

#### إذا كانت القاعدة موجودة:
```bash
mysql -u username -p database_name < database/migrations/add_service_type_tier_to_pricing_plans.sql
```

**أو من phpMyAdmin:**
1. افتح phpMyAdmin
2. اختر قاعدة البيانات
3. تبويب "SQL"
4. انسخ والصق محتوى الملف المناسب
5. اضغط "Go"

---

### الخطوة 2: تحقق من النجاح (دقيقة واحدة)

```sql
DESCRIBE pricing_plans;
```

**يجب أن ترى:**
- ✅ `service_type` - ENUM('website', 'app', 'both')
- ✅ `tier` - ENUM('basic', 'professional', 'enterprise')

---

### الخطوة 3: ابدأ الاستخدام! (دقيقتان)

1. **سجل دخول إلى Admin Panel:**
   ```
   URL: http://your-domain.com/admin
   Email: admin@ashub.com
   Password: Admin@123
   ```

2. **اذهب إلى Pricing Plans:**
   ```
   http://your-domain.com/admin/pricing
   ```

3. **أضف خطة جديدة:**
   - اضغط "Add New Plan"
   - املأ النموذج
   - اضغط "Create Plan"

---

## ✅ تم! 🎉

الآن يمكنك:
- ✅ إضافة خطط أسعار بسهولة
- ✅ تعديل الخطط الموجودة
- ✅ فلترة حسب نوع الخدمة والمستوى
- ✅ إدارة كاملة من لوحة التحكم

---

## 📚 للمزيد من التفاصيل

راجع الملفات التالية:
- **`PRICING_SYSTEM_AUDIT.md`** - تقرير فحص شامل
- **`PRICING_SETUP_GUIDE.md`** - دليل إعداد مفصل

---

## 🆘 مشاكل؟

### "Column not found"
```sql
-- نفذ هذا:
ALTER TABLE pricing_plans 
ADD COLUMN service_type ENUM('website', 'app', 'both') NOT NULL DEFAULT 'website' AFTER language,
ADD COLUMN tier ENUM('basic', 'professional', 'enterprise') NOT NULL DEFAULT 'basic' AFTER service_type;
```

### "Unauthorized"
- تأكد من تسجيل الدخول
- تحقق من صلاحية الـ Token

---

## 🎊 مبروك!

نظام خطط الأسعار جاهز للاستخدام! 🚀
